# Copy this file to ".env" and fill in your Supabase project values.
# The app runs with built-in fallback product data even if these are left blank,
# so you can develop the frontend before wiring up Supabase.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
